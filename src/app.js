import Vue from 'vue';

document.addEventListener('DOMContentLoaded', () => {
  new Vue ({
    el: "#app",
    data: {
      todos: ["Unload dishwasher", "Load laundry", "Put away shopping"],
    },
    // methods: {

    // },
  });
});